services:
  - type: web
    name: audiomax-backend
    env: node
    buildCommand: npm install --production
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production